########
# defconfig has these as y but Ross has them as m, 
# force y to help debug (directly mount sd card w/o initrd)

# MMC
# don't do this
#CONFIG_MMC_SDHCI_OF_ARASAN=y

# Create /dev/i2c-* dev nodes w/o modprobe
CONFIG_I2C_CHARDEV=y

